import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ResponderDashboardPage } from './responder-dashboard.page';

describe('ResponderDashboardPage', () => {
  let component: ResponderDashboardPage;
  let fixture: ComponentFixture<ResponderDashboardPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ResponderDashboardPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});




