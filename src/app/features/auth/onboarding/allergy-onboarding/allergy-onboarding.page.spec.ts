import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AllergyOnboardingPage } from './allergy-onboarding.page';

describe('AllergyOnboardingPage', () => {
  let component: AllergyOnboardingPage;
  let fixture: ComponentFixture<AllergyOnboardingPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AllergyOnboardingPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});




